{
  "name": "@pacelore/metrics",
  "version": "0.0.0",
  "private": true,
  "description": "Training metrics — TSS, NP, IF, GAP, peak curves, PMC.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "vitest": "2.1.5"
  }
}
