{
  "name": "@open-strava/metrics",
  "version": "0.0.0",
  "private": true,
  "description": "Training metrics — TSS, NP, IF, GAP, peak curves, PMC.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "echo \"metrics tests — wired in T08\" && exit 0",
    "typecheck": "echo \"metrics typecheck — wired in T08\" && exit 0"
  }
}
