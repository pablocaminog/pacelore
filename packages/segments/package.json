{
  "name": "@open-strava/segments",
  "version": "0.0.0",
  "private": true,
  "description": "Segment matching — R-tree bbox prefilter + DTW polyline match.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "echo \"segments tests — wired in T13\" && exit 0",
    "typecheck": "echo \"segments typecheck — wired in T13\" && exit 0"
  }
}
