{
  "name": "@open-strava/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@open-strava/fit-parser": "workspace:*",
    "@open-strava/metrics": "workspace:*",
    "@open-strava/segments": "workspace:*",
    "@simplewebauthn/server": "11.0.0",
    "@simplewebauthn/types": "11.0.0",
    "hono": "4.6.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20241127.0",
    "vitest": "2.1.5",
    "wrangler": "3.91.0"
  }
}
