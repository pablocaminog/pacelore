{
  "name": "@pacelore/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pacelore/fit-parser": "workspace:*",
    "@pacelore/metrics": "workspace:*",
    "@pacelore/planner": "workspace:*",
    "@pacelore/segments": "workspace:*",
    "@simplewebauthn/server": "11.0.0",
    "@simplewebauthn/types": "11.0.0",
    "fflate": "0.8.2",
    "hono": "4.6.10",
    "jszip": "3.10.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20241127.0",
    "vitest": "2.1.5",
    "wrangler": "4.41.0"
  }
}
